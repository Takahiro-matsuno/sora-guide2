<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.jalinfotec.kankoannai">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".KankoMainActivity1">
        </activity>
        <activity android:name=".KankoMainActivity2">

            <!-- intetfilterが動かす画面の対象になる -->

        </activity>
        <activity android:name=".TopMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".AirportGuideActivity">
        </activity>
        <activity android:name=".TaxiReservationActivity">
        </activity>
        <activity android:name=".MyTaxiActivity">
        </activity>
        <activity android:name=".TaxiReservationCompActivity">
        </activity>
    </application>

</manifest>